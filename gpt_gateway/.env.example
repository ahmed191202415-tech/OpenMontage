# Long random secret used by the Custom GPT Action.
GPT_GATEWAY_API_KEY=replace-me-with-a-long-random-secret

# Public HTTPS base URL used in the GPT Action schema after deployment.
GPT_GATEWAY_PUBLIC_URL=https://your-openmontage-domain.example.com

# Provider keys remain configured in OpenMontage's root .env.
